
// MVP: converts image to rough polygon via manual points (stub)
export function detectShapeFromImage(img) {
  // In real system: OpenCV.js edge detection
  return [
    { x: 0, y: 0 },
    { x: 200, y: 0 },
    { x: 200, y: 120 },
    { x: 0, y: 120 }
  ];
}
